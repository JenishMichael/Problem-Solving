//Sorting a ball based on colorsCreate an object with class name as ball give attributes namely color. 
//And Sort the ball based on colour in ascending and descending 
class Ball {
  String color;

  Ball(this.color);

  String toString(){
    return "Color: $color";
  }
}

